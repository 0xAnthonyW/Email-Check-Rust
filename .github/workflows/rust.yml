name: Rust Console CI/CD

on:
  push:
    branches: [ "master" ]

permissions:
  contents: write
  actions: read

jobs:
  build-and-release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            ext: ""
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            ext: ".exe"
          - os: macos-latest
            target: x86_64-apple-darwin
            ext: ""

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Rust (stable) with target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build all binaries
        run: |
          cargo build --release --target ${{ matrix.target }} --bin email-check-rust
          cargo build --release --target ${{ matrix.target }} --bin bulk

      - name: Prepare artefact directory (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p dist
          cp "target/${{ matrix.target }}/release/email-check-rust${{ matrix.ext }}" dist/
          cp "target/${{ matrix.target }}/release/bulk${{ matrix.ext }}" dist/

      - name: Prepare artefact directory (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path dist | Out-Null
          Copy-Item "target\${{ matrix.target }}\release\email-check-rust${{ matrix.ext }}" dist\
          Copy-Item "target\${{ matrix.target }}\release\bulk${{ matrix.ext }}" dist\

      - name: Compress artefact (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd dist
          zip -r "../release-${{ matrix.target }}.zip" *

      - name: Compress artefact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path dist\* -DestinationPath "release-${{ matrix.target }}.zip"

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: "release-${{ github.run_number }}-${{ matrix.target }}-${{ github.sha }}"
          release_name: "Email-Check-Rust ${{
            github.run_number }} (${{ matrix.target }})"
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: "release-${{ matrix.target }}.zip"
          asset_name: "release-${{ matrix.target }}.zip"
          asset_content_type: application/zip
